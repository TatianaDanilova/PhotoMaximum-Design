<Page x:Class="Photo_Maximum.MasterPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Страница мастера" Background="#BDC3C7" Width="850" Height="500" >
    <Page.Resources>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="#34495E"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="6 3"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0 10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                        Background="{TemplateBinding Background}"
                        CornerRadius="2"
                        BorderBrush="#34495E"
                        BorderThickness="1"
                        Padding="8 4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#34495E"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#34495E"/>
                                <Setter Property="BorderBrush" Value="#34495E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <StackPanel Orientation="Horizontal">
            <Button Content="Назад" Style="{StaticResource PrimaryButtonStyle}" Click="BackButton_Click" HorizontalAlignment="Left"/>
            <TextBlock Text="Мои заказы" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>
        </StackPanel>
       

        <!-- Список заказов -->
        <DataGrid x:Name="OrdersGrid" 
                  Grid.Row="1" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True" 
                  Margin="0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding RequestId}" Width="Auto"/>
                <DataGridTextColumn Header="Тип" Binding="{Binding TypeName}" Width="*"/>
                <DataGridTextColumn Header="Размер" Binding="{Binding Size}" Width="Auto"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="Auto"/>
                <DataGridTextColumn Header="Клиент" Binding="{Binding ClientName}" Width="Auto"/>
                <DataGridTextColumn Header="Дата начала" Binding="{Binding DateStart, StringFormat='dd.MM.yyyy'}" Width="Auto"/>
                <DataGridTextColumn Header="Дата завершения" Binding="{Binding DateEnd, StringFormat='dd.MM.yyyy'}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Кнопки для управления заказами -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10">
            <Button Content="Подтвердить заказ" 
                    Style="{StaticResource PrimaryButtonStyle}" 
                    Margin="5,0" 
                    Click="ConfirmOrder_Click"/>
            <Button Content="Начать выполнение" 
                    Style="{StaticResource PrimaryButtonStyle}" 
                    Margin="5,0" 
                    Click="StartOrder_Click"/>
            <Button Content="Завершить заказ" 
                    Style="{StaticResource PrimaryButtonStyle}" 
                    Margin="5,0" 
                    Click="CompleteOrder_Click"/>
            <Button Content="Отказаться от заказа" 
                    Style="{StaticResource PrimaryButtonStyle}" 
                    Margin="5,0" 
                    Click="RejectOrder_Click"/>
        </StackPanel>
    </Grid>
</Page>